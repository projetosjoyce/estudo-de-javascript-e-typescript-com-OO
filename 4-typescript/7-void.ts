/* VOID
- O void é o oposto de Any geralmente declarado em funções pois em variavéis não tem muita utilidade já que acc apenas undefined;
*/

function logMessage(): void {
    console.log('Mensagem do sistema!');
}

logMessage();