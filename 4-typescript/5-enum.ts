/* ENUM
- O enum nos dá a possibilidade de criar um conjunto apenas com valores numéricos
- Não consigo alterar o valor depois de declarado
*/

enum Carro { Motor = 1, Portas = 4, Pedais = 3}

let numeroDeMotores = Carro.Motor;

console.log(Carro);
console.log(numeroDeMotores);